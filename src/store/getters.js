let getters={
  // bNav:(state)=>state.bNav,
  bCoom:(state)=>state.bCoom,
  // bLoading:(state)=>state.bLoading
  homepage:(state)=>state.homepage,
  index:(state)=>state.index
};
export default getters;